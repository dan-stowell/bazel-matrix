# Activate after //projects/googleapis/as_is is active and builds locally.
#
# load("//bazel_runner:defs.bzl", "hermetic_llvm_project_modification")
# load("//projects/googleapis:project.bzl", "GOOGLEAPIS_PROJECT")
#
# hermetic_llvm_project_modification(project = GOOGLEAPIS_PROJECT)
