# Activate after //projects/livegrep/as_is is active and builds locally.
#
# load("//bazel_runner:defs.bzl", "hermetic_llvm_project_modification")
# load("//projects/livegrep:project.bzl", "LIVEGREP_PROJECT")
#
# hermetic_llvm_project_modification(project = LIVEGREP_PROJECT)
