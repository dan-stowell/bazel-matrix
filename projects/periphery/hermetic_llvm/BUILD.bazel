# Activate after //projects/periphery/as_is is active and builds locally.
#
# load("//bazel_runner:defs.bzl", "hermetic_llvm_project_modification")
# load("//projects/periphery:project.bzl", "PERIPHERY_PROJECT")
#
# hermetic_llvm_project_modification(project = PERIPHERY_PROJECT)
