# Activate after //projects/sonnet/as_is is active and builds locally.
#
# load("//bazel_runner:defs.bzl", "hermetic_llvm_project_modification")
# load("//projects/sonnet:project.bzl", "SONNET_PROJECT")
#
# hermetic_llvm_project_modification(project = SONNET_PROJECT)
