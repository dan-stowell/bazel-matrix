# Activate after //projects/tensorflow/as_is is active and builds locally.
#
# load("//bazel_runner:defs.bzl", "hermetic_llvm_project_modification")
# load("//projects/tensorflow:project.bzl", "TENSORFLOW_PROJECT")
#
# hermetic_llvm_project_modification(project = TENSORFLOW_PROJECT)
