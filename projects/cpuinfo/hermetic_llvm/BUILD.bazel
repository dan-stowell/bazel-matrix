# Activate after //projects/cpuinfo/as_is is active and builds locally.
#
# load("//bazel_runner:defs.bzl", "hermetic_llvm_project_modification")
# load("//projects/cpuinfo:project.bzl", "CPUINFO_PROJECT")
#
# hermetic_llvm_project_modification(project = CPUINFO_PROJECT)
