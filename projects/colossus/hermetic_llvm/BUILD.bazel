# Activate after //projects/colossus/as_is is active and builds locally.
#
# load("//bazel_runner:defs.bzl", "hermetic_llvm_project_modification")
# load("//projects/colossus:project.bzl", "COLOSSUS_PROJECT")
#
# hermetic_llvm_project_modification(project = COLOSSUS_PROJECT)
